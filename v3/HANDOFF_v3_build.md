# MiniBox Master Toolkit v3.0 — Build Handoff

## UPDATE 2026-07-07 — Cardiology tab: playbook embedded + DEPLOYED (v3.15, sw mbx-2026-07-07a)
- The Cardiology tab (added earlier 2026-07-07) now has a Rheumatology-style sub-nav: **📖 The Playbook** (default view — the full interactive MiniBox Cardiology Playbook from `..\minibox-cardiology-playbook.html`, embedded whole as an escaped-`srcdoc` iframe like the RMA/ROI builders; it keeps its own Rep-Playbook / "Show the Doctor" toggle and live ROI dials; a small injected script postMessages `cardioPbHeight` so the iframe auto-sizes) + **🔬 Evidence & Billing** (the existing sourced evidence/billing/files content, unchanged, wrapped in `#cardio-view-evidence`). Switch fn = `cardioShowView()` next to `rheumShowView()`.
- ROOT CAUSE of "the app doesn't show Cardiology": the Cardiology tab was added to LATEST only — `_deploy_toolkit/index.html` was still the pre-cardiology mirror, so the installed app kept serving the old build. Fixed by mirroring LATEST + materials-data.js + `materials/Cardiology-Complete-PFT-Clinical-Billing.pdf` into `_deploy_toolkit`, bumping sw.js to `mbx-2026-07-07a`, `vercel --prod --yes`. VERIFIED LIVE: password gate 303, served page byte-identical to LATEST, v3.15 comment + cardio iframe present, sw at 07-07a (installed apps get the blue Refresh bar). **REMINDER: every LATEST edit isn't live until the mirror+bump+deploy routine runs.**
- Gotchas hit (don't repeat): JS `String.replace` treats `$'`/`$&` in the replacement as special — the playbook's `'$' + …` code spliced ~700KB of the doc into itself on the first attempt (use the function-form replacement); PowerShell `-replace` round-trip also mojibake'd the file's em-dashes/emoji (edit via Node/Write, not Get-Content|Set-Content).
- Backup: `MiniBox Sales Toolkit (LATEST).html.pre-cardio-playbook.bak`. NOTE: playbook's code table bills 94060+94726+94729 = $147.18 vs the evidence view's 94010 bundle = $151.45 — different bundles (pre/post bronchodilator vs plain spirometry), both left as delivered on purpose.

## UPDATE 2026-07-06 — Hosted app auto-update wiring is LIVE (⚠️ mirror rule below)
- The hosted edition at **https://deploytoolkit.vercel.app** (password `minibox2026`) is now a real installable app: manifest + apple-touch-icon + service-worker registration + blue "A new version of the toolkit is ready — Refresh" bar + Android/desktop Install bar.
- The wiring lives INSIDE the toolkit master as a `<script id="mbx-pwa">` block just before `</body>` in `v3/MiniBox Sales Toolkit (LATEST).html`. It self-disables on `file://`, so downloaded offline copies are unaffected. **⚠️ WHEN MIRRORING LATEST → `_deploy_toolkit/index.html`, KEEP THIS BLOCK** (it's in the master now, so a plain byte-for-byte copy is correct — just never strip it). It was missing before today because an earlier mirror overwrote the deploy copy without it.
- DEPLOY ROUTINE: edit LATEST → copy to `_deploy_toolkit/index.html` → bump `VERSION` in `_deploy_toolkit/sw.js` (e.g. `mbx-2026-07-06e` → next date/letter) → `vercel --prod --yes` from `_deploy_toolkit`. Verified live 2026-07-06: password gate 401→303, wired page served, sw.js at `mbx-2026-07-06e`, manifest+icons 200.
- Updates reach reps two ways: page shell is network-first (every online open fetches the newest toolkit), and the sw.js version bump triggers the Refresh bar for anyone mid-session/offline-cached. The offline .zip copies can never auto-update — steer reps to the installed app.
_Written 2026-06-11, updated same day: **THE BUILD IS DONE AND VERIFIED.** Erik is non-technical — plain English, no jargon._

## STATUS: v3.0 complete (2026-06-11)
- `v3/index.html` — the finished toolkit. All v2.27 content preserved; email machinery rebuilt. Verified in a live browser: zero console errors, all legacy calculators intact, Mode A builds accurate package emails, .eml drafts contain genuine PDF attachments (validated MIME structure, %PDF magic bytes, X-Unsent draft flag, body round-trips), Mode B chips work and preserve typed To/Cc, Outlook-flavor highlighting + profile persistence work, Materials Library renders all 17 files ("Email it" jumps to a pre-selected Mode A).
- `v3/MiniBox_Toolkit_v3_offline.zip` (17.9 MB) — the distributable offline copy (index.html + materials-data.js + materials/ + README-OFFLINE.txt). KEEP index.html and materials-data.js together.
- User-friendliness round (same day, all verified in browser, zero console errors): first-open 30-second welcome modal (name/email/which-Outlook → auto-signs everything, shows once via localStorage `mbx3_welcome_done`); one-tap Quick Packs in Mode A (Intro/Evidence/IT/Closing/Clear); "🧪 Test it on yourself" practice-run button (real .eml with brochure addressed to the rep's own inbox); Mode A output body is now editable before sending (line breaks survive — m3EasBody parses contenteditable); Materials Library is covered by the global search automatically (search reads live DOM). Offline zip rebuilt with all of it.
- RHEUMATOLOGY FOLDER (2026-06-15, verified in browser, zero errors): the old "Rheum Estimator" tab is now a full **Rheumatology** resource center with a 3-way sub-nav — **The Playbook** (the 5 moves, patient math, CPT bundle, monitoring cadence, 3 objections, financing, 4 discovery questions — copy lifted verbatim from the Rheum Playbook.zip), **PFT Estimator** (the original calculator, untouched, still computes), and **Files & Templates** (the 7 playbook files: View/Download/"Email it", plus "Email the whole rheum pack"). Source = C:\Users\erikb\Downloads\Rheum Playbook.zip; its 7 embedded base64 files were extracted into v3/materials/ (01_Rheumatology_Play_Segment_Slick.pdf … 07_Email_Templates_Sales_Call_Guide.docx). They're registered in MATERIALS under a new `cat:'rheum'` category, so they also appear in the Forms-tab Materials Library and as a "🦴 Rheum Pack" quick-pack in the Email Center. Engine upgrade: m3Mime() now sets correct Content-Type for .docx/.pptx (not just PDF) in the .eml builder, m3View downloads Office files instead of opening a blank tab, M3_CAT_ORDER drives category rendering. build-materials-data.ps1 + materials-data.js (now 11.3 MB, 23 embedded files) + offline zip (20.9 MB) all rebuilt.
- REMAINING (needs Erik): (1) real-world test — open the .eml in his actual Outlook; (2) HOSTING — not deployed yet on purpose: publishing company materials (incl. W-9/bank info) to a URL needs Erik's go-ahead + a password gate. Plan: new private repo + Vercel static project + basic-auth edge middleware; hosted page's "get offline copy" badge already links to MiniBox_Toolkit_v3_offline.zip. Future ideas pitched to Erik: shared template/win-wire exchange between reps, usage counter, install-to-home-screen like the Dragons PWA, auto "new version available" banner on the hosted edition.

## UPDATE 2026-06-25 — "All files as one .zip" (no-IT attachment fix)
- Newest working file is `MiniBox_Toolkit_v3.3.html`. Erik reported "attachments aren't attaching" + asked to hook send directly to erik.bernard@pulm-one.com. Root cause (not a bug): the `.eml` draft DOES attach for real, but only opens as a sendable draft in **Classic** Outlook; New Outlook / Outlook-web open `.eml` read-only. mailto/OWA paths never attach by design.
- Chose the no-IT, works-today path. Added a **pure-JS store-mode ZIP builder** (`m3BuildZip`/`m3Crc32`/`m3DownloadZip`, just before `m3EmlAvailable()`), so every selected PDF bundles into ONE `.zip` the rep drags into any email in ANY Outlook. New button **"📦 All files as one .zip"** in Mode A send row (`m3EasDownloadZip`) and Mode B chips send row (`m3ElZip`, shows only when materials selected). No dependency, stays offline. VERIFIED in Node against 2 real materials: `unzip -t` clean, extracted bytes identical to originals (CRC32 good).
- TRUE auto-send from his account (button just sends, no drag) still requires **Microsoft Graph + an Azure app sign-in that PulmOne IT must approve** — explained to Erik, parked until he gets IT's OK. That's the only real "hook directly into the account" path.
- TODO if distributing offline copies: rebuild `MiniBox_Toolkit_v3_offline.zip` so the offline edition carries the new zip button (current offline zip predates it).

## ✅ RESOLVED 2026-06-25 — Erik is a 100% Outlook-ON-THE-WEB user
- THE missing fact behind all his attachment pain: Erik works entirely in **Outlook on the web** (outlook.office365.com, signed in as Erik.Bernard@pulm-one.com). **The `.eml` "Outlook Draft" button is useless for him** — web Outlook physically cannot open a local `.eml` as a draft (desktop-only feature). Don't send him down the .eml path again.
- WORKING WORKFLOW (verified — he sent a real 3-attachment email to himself, "MiniBox+ Resources for james pulmonary", 719 KB): generate email in toolkit → click **📦 All files as one .zip** (or attach loose PDFs) → click **🌐 Outlook Web** to open the pre-written compose in the web → **drag the file(s) in** (or paperclip → Browse this computer) → Send.
- Acrobat-popup side-quest cause: Chrome's **"Always open with system viewer"** was on for PDFs, so every loose-PDF download auto-launched Acrobat and buried the compose window. Unchecking it stops that; the zip path avoids it entirely (zips don't auto-open).
- Open offer to Erik: (1) make the toolkit auto-bundle a zip on every generate (one less click); (2) the only true zero-drag "just send from my pulm-one account" path is **Microsoft Graph send**, which needs PulmOne IT to approve an app sign-in once — parked until Erik pursues IT.

## The mission (Erik's words)
The toolkit (`MiniBox_Master_Toolkit_v2.27.html`, one folder up) is the one-stop shop for PulmOne MiniBox+ sales reps. **Everything needs to work** — especially email: today, "send" buttons produce emails that SAY "please find attached" but nothing is ever attached. Erik approved a **full v3.0 rebuild**, delivered **both** as a hosted private web page AND a downloadable offline copy. Reps are on **mixed Outlook setups** (classic desktop / New Outlook / web — design for all three).

## Diagnosis of v2.27 (already done — don't redo)
1. `mailto:` and Outlook-Web deeplinks physically cannot attach files. All Mode A / Mode B / EAS send buttons use them → attachments never sent.
2. Mode A's generated body claims materials are "attached to this email" while the actual attach step is a human checklist — the embarrassing failure Erik described.
3. The only real-attachment path is the `.eml` draft download (Proposal Builder) — works in classic Outlook only; New Outlook opens .eml read-only.
4. The materials themselves (brochure, studies, CPT sheet…) did not exist inside the tool at all.
5. Other fragile extras: long bodies silently truncated by mailto/OWA URL limits; .docx import needs a CDN at click time; templates/Rep Profile live in one browser's localStorage; embedded ROI/Quote builder iframes carry frozen copies of pricing (see `../VERIFY_rates_and_claims.md`).

## What's already built (state right now)
- `v3/materials/` — 17 curated PDFs with clean customer-facing names, copied from Erik's OneDrive sales folders (brochure, Why-MiniBox, CHEST 2021 summary + full article, Verona study, sample PFT report, coaching sheet, competitive comparison, manufacturer certificate, HL7 2025, MDS2, financing application 2025, W-9, bank info, DLCO gas requirements, ACH form PDF, user manual Rev 20).
- `v3/build-materials-data.ps1` — regenerates `materials-data.js`.
- `v3/materials-data.js` — 9.1 MB, base64 of every PDF except the 8.5 MB user manual (link-only). This is what lets the tool build REAL Outlook drafts (.eml) with genuine PDF attachments, online or offline.
- `v3/index.html` — fresh copy of v2.27, renovation NOT yet started.

## The v3.0 design (agreed direction)
- Keep ALL existing content/numbers from v2.27 (VERIFY doc forbids changing clinical/financial figures). This is a renovation of the machinery, not a content rewrite.
- New `MATERIALS` registry (id, title, description, filename, KB, category, `public` flag). Sensitive items (bank info, W-9, ACH) get `public:false` → never linked in email bodies, attachment-only.
- **Email engine** (new script at end of index.html):
  - `buildEmlWithAttachments(to, cc, subject, body, ids)` → downloads a ready-to-send Outlook draft (.eml, X-Unsent: 1) with the selected PDFs genuinely attached (base64 from MATERIALS_B64, 76-char line chunking, multipart/mixed).
  - OWA path: open compose deeplink + auto-download selected PDFs + clear "drag these files in" guidance. When hosted (location.protocol is http/https), public materials become LINKS in the body instead.
  - Body wording adapts: says "attached" ONLY on the .eml path; otherwise says "linked below" / "files downloaded alongside". Never promise an attachment that isn't there.
  - mailto/OWA length guard: if body > ~1800 chars, auto-copy body + warn.
- **Mode A (Email Assembly Station)**: replace fake material toggles (roi/cpt/studies list) with real material checkboxes from the registry, show total MB, three send options (Outlook draft w/ attachments • Outlook Web + downloads • Copy).
- **Mode B (template library)**: add an "attach materials" chip row to the preview; same three send paths.
- **Materials Library** grid in the Forms tab: View / Download / Email per file.
- **Rep Profile**: add "Which Outlook do you use?" (classic / new / web / not sure) → email panels highlight the right button.
- Header: bump to v3.0; show online/offline indicator; hosted version offers "Download offline copy" (zip of v3 folder, built at the end).
- Proposal/Quote builder iframes: leave content; their .eml path already attaches the proposal HTML. Fix wording only if low-risk (their JS is HTML-escaped inside `srcdoc` — edit carefully or skip for v3.0 and note it).
- Hosting (after the build works locally): new private repo + static host with a simple password gate; NOT part of the Dragons site. Erik must approve anything public; bank/W-9/ACH should not be on a public URL without the gate.

## Working rules from Erik
- Ask questions when scoping; otherwise act autonomously and announce clearly when DONE.
- No browser screenshots to prove changes (verify via DOM checks); show previews as inline images only if asked.
- Plain-English explanations.
